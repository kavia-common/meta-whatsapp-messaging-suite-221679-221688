import React from 'react';

/**
 * MessagingPage - Placeholder for sending bulk messages using selected template and contacts.
 */
// PUBLIC_INTERFACE
export default function MessagingPage() {
  return (
    <div className="page" role="region" aria-label="Messaging">
      <h1>Messaging</h1>
      <p className="muted">
        Select a template and a contact list to send messages. Scheduling and preview coming soon.
      </p>
    </div>
  );
}
